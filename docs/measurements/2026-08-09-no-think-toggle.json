{
  "measured": "2026-08-09",
  "model": "Qwen3-VL-30B-A3B-Thinking-FP8",
  "question": "Does /no_think or /nothink in the prompt suppress reasoning on this checkpoint?",
  "answer": "No. It is not a toggle here — it is ordinary prompt text.",
  "template_evidence": {
    "file": "models/llm/Qwen3-VL-30B-A3B-Thinking-FP8/chat_template.json",
    "mentions_enable_thinking": false,
    "no_think_branch": false,
    "generation_prompt": "{{- '<|im_start|>assistant\\n<think>\\n' }}  — unconditional",
    "note": "The /no_think switch belongs to the Qwen3 HYBRID checkpoints. The Qwen3-VL *Thinking* checkpoints are thinking-only and their template has no branch for it, so the block always opens."
  },
  "empirical_reasoning_chars": {
    "task": "reverse the arrows in tests/fixtures/dependency_input.drawio, production vision prompt",
    "baseline_3_runs": [
      7532,
      8093,
      5042
    ],
    "nothink_3_runs": [
      12319,
      9437,
      10811
    ],
    "no_think_3_runs": [
      5097,
      6245,
      7511
    ],
    "verdict": "Overlapping ranges, and /nothink came out ABOVE baseline in all three runs. No suppression; the extra token may itself invite deliberation."
  },
  "enable_thinking_kwarg": "chat_template_kwargs {'enable_thinking': false} empties the reasoning FIELD but not the reasoning: the template ignores the variable, so the model still emits its chain of thought, and vLLM simply stops routing it to the parser — the deliberation lands in `content` instead. That is worse than leaving it on.",
  "consequence": "Removing the reasoning cost on this box means running the Instruct checkpoint (registry index 0), not a prompt directive."
}
